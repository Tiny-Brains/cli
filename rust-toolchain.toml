# EXACT. The check workflow, the release workflow and a local build all compile with this rustc, so
# the binary on a release was built by the compiler this repository was tested with.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
